import { CoreDirectivesDragAndDropDirective } from './core-directives-drag-and-drop.directive';

describe('CoreDirectivesDragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new CoreDirectivesDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
