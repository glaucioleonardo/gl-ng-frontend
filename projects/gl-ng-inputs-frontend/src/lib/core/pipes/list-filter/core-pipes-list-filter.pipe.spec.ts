import { CorePipesListFilterPipe } from './core-pipes-list-filter.pipe';

describe('CorePipesListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CorePipesListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
